import React from "react";
import { Text } from "components/Text";
import { TextDefaultTheme } from "components/Text/Theme";

export const Login = () => {
  return (
    <div>
      <Text theme={TextDefaultTheme}>login!!!</Text>
    </div>
  );
};
